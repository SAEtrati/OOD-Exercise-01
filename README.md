# به نام خداوند جان و خرد
#### اصول طراحی شی‌گرایی سیستم‌ها | تمرین ۱
پیاده‌سازی و بازطراحی یک سیستم ساده‌ی رزرو هتل بر اساس اصول شی‌گرایی (SRP, OCP, LSP, ISP, DIP, PLK, CRP).

---

## 👥 اعضای گروه

- سید علی عترتی - [@SAEtrati](https://github.com/SAEtrati)
- سید محمد عترتی - [@SMEtrati](https://github.com/SMEtrati)

---

## 🗂️ ساختار کلی مخزن

- فاقد اصول [`Step-01-without-OOD/`](./Step-01-without-OOD)
  نسخه‌ی توسعه‌یافته‌ی کد اولیه برای **گام اول**؛ در این نسخه فقط رفتار سیستم گسترش داده شده و طراحی اصلی عمداً دست‌نخورده مانده است.

- مطابق اصول [`Step-02-with-OOD/`](./Step-02-with-OOD)
  نسخه‌ی بازطراحی‌شده برای گام‌های بعدی (اعمال اصول شی‌گرایی). توضیحات مربوط به آن در گام ۲ و ۳ همین ReadMe قرار دارد.

- توضیحات `README.md`
  گزارش متنی تمرین برای همه‌ی گام‌ها (این فایل).

---

## 1️⃣ گام اول: افزودن قابلیت‌های جدید بدون بازطراحی (`Step-01-without-OOD`)

در گام اول، هدف این است که روی طراحی فعلی سیستم هیچ بازطراحی خاصی انجام نشود و تنها **رفتار سیستم رزرو هتل گسترش پیدا کند**. دو قابلیت جدید که باید به سیستم اضافه شوند:

1. **ارسال پیامک (SMS)** برای تأیید رزرو، در کنار ارسال ایمیل.
2. **پرداخت حضوری (Onsite)** در کنار سایر روش‌های پرداخت موجود مثل پرداخت نقدی، کارت و PayPal.

به‌عبارت دیگر، پس از اجرای گام اول:

- سیستم می‌تواند تأیید رزرو را هم به‌صورت **ایمیل** و هم به‌صورت **SMS** برای مشتری ارسال کند.
- مشتری می‌تواند نوع پرداخت را روی **پرداخت حضوری در محل** نیز تنظیم کند.

> [!IMPORTANT]
> تمام تغییرات این مرحله فقط در پوشه‌ی `Step-01-without-OOD` اعمال شده‌اند و ساختار کلی طراحی همان ساختار پروژه‌ی اولیه باقی مانده است. مقایسه‌ی تعداد و نوع این تغییرات با طراحی بازطراحی‌شده‌ی گام سوم، موضوع اصلی گام چهارم خواهد بود.

---

### ۱. کارهای انجام‌شده در گام اول

#### ۲.۱. قابلیت ارسال پیامک (SMS)

برای اضافه‌کردن SMS به سیستم اعلان رزرو:

- یک متد جدید برای ارسال پیامک به **اینترفیس/کلاس مسئول ارسال پیام** اضافه شده است تا علاوه‌بر ایمیل، کانال SMS هم پشتیبانی شود.
- یک کلاس جداگانه برای **ارسال پیامک** پیاده‌سازی شده است که متن تأیید رزرو را به شماره‌ی موبایل مشتری ارسال می‌کند.
- در کلاس سرویس رزرو، منطق انتخاب نوع اعلان (ایمیل یا SMS) اضافه شده است تا بر اساس مقدار `Notifier`، کلاس مناسب فراخوانی شود.

#### ۲.۱. قابلیت پرداخت حضوری (Onsite Payment)

برای اضافه‌کردن روش پرداخت حضوری:

- در کلاس مسئول پرداخت، یک متد جدید برای ثبت **پرداخت حضوری** اضافه شده است تا در کنار روش‌های قبلی (نقدی، کارت، PayPal) قابل استفاده باشد.
- در کلاس سرویس رزرو، منطق انتخاب روش پرداخت توسعه داده شده است تا مقدار مربوط به پرداخت حضوری نیز در کنار سایر روش‌ها پشتیبانی شود.

---

### ۲. جدول تغییرات گام اول

در جدول زیر، مهم‌ترین تغییراتی که برای اضافه‌کردن دو قابلیت «ارسال پیامک» و «پرداخت حضوری» اعمال شده‌اند، به‌صورت خلاصه آمده است.  
هر ردیف، مطابق تعریف بخش ۱، نماینده‌ی یک **تغییر** در کد است.

> [!NOTE]
> نام کلاس‌ها بر اساس ساختار پروژه در پوشه‌ی `Step-01-without-OOD` نوشته شده است.

| بخش ارسال پیام (SMS) | توضیح تغییر در ارسال پیام | بخش پرداخت (Onsite) | توضیح تغییر در پرداخت | ردیف |
|----------------------|---------------------------|----------------------|------------------------|---|
| `MessageSender`      | افزودن متد جدید برای ارسال پیامک (در کنار متد ارسال ایمیل) تا اینترفیس ارسال پیام از SMS نیز پشتیبانی کند. | `PaymentProcessor`    | افزودن متد جدید برای پرداخت حضوری در محل، در کنار متدهای قبلی پرداخت (کارت، نقدی، PayPal و ...). | ۱ |
| `EmailSender`        | به‌روزرسانی کلاس ارسال ایمیل برای پیاده‌سازی متد جدید اینترفیس و هم‌خوانی با تعریف تازه‌ی `MessageSender`. | `ReservationService`  | توسعه‌ی منطق رزرو برای استفاده از `PaymentProcessor` و فراخوانی متد پرداخت حضوری هنگام انتخاب این روش. | ۲ |
| `SmsSender`          | ایجاد کلاس جدید برای ارسال SMS که پیام تأیید رزرو را به شماره‌ی موبایل مشتری ارسال می‌کند. | –                | – | ۳ |
| `ReservationService` | افزودن منطق انتخاب نوع اعلان بر اساس مقدار `Notifier` و استفاده از `SmsSender` در صورت انتخاب SMS. | –                    | – | ۴ |

---

## 2️⃣ گام دوم: تحلیل اصول شی‌گرایی روی کد اولیه

در این گام، نسخه‌ی اولیه‌ی سیستم (کدی که مبنای شروع کار در گام اول بوده است، قبل از اضافه‌کردن SMS و پرداخت حضوری) از نظر اصول شی‌گرایی بررسی شده است. برای هر اصل، یک نمونه‌ی **برقراری** و یک یا چند نمونه‌ی **نقض** در کد مشخص شده است.

### ۱. اصول مورد بررسی

- **SRP** – Single Responsibility Principle  
- **OCP** – Open/Closed Principle  
- **LSP** – Liskov Substitution Principle  
- **ISP** – Interface Segregation Principle  
- **DIP** – Dependency Inversion Principle  
- **PLK** – Principle of Least Knowledge (Law of Demeter)  
- **CRP** – Common Reuse Principle  

---

### ۲. تحلیل SRP – اصل تک‌مسئولیتی

| توضیح | کلاس/محل | نوع |
|--------|-----------|-----|
| این کلاس‌ها فقط داده‌های مربوط به خود (مشتری، اتاق و رزرو) را نگه می‌دارند و در حد نیاز محاسبه‌ی ساده (مثل `totalPrice`) انجام می‌دهند؛ هرکدام تنها یک دلیل برای تغییر دارند. | `Customer`, `Room`, `Reservation` | ✔️ مورد برقراری |
| این کلاس هم تخفیف شهر خاص (مثل پاریس) را اعمال می‌کند، هم پرداخت را مدیریت می‌کند، هم پیام تأیید را ارسال می‌کند و هم خلاصه‌ی رزرو را چاپ می‌کند. یعنی چند مسئولیت متفاوت در یک کلاس. | `ReservationService` | ❌ مورد نقض |

---

### ۳. تحلیل OCP – اصل باز/بسته بودن

| توضیح | کلاس/محل | نوع |
|--------|-----------|-----|
| استفاده از enum به‌جای رشته‌های خام، توسعه را منظم‌تر کرده و از اشتباهات متنی جلوگیری می‌کند. | استفاده از `enum`ها مثل `PaymentMethods` و `Notifier` | ✔️ مورد برقراری |
| برای افزودن یک روش پرداخت یا نوع اعلان جدید، باید چندین کلاس موجود تغییر کنند (افزودن مقدار به enum، تغییر در `switch`های رزرو و پرداخت، تغییر در منطق برنامه‌ی اصلی). بنابراین کلاس‌ها برای توسعه بسته نیستند. | `ReservationService`, `PaymentProcessor`, `Main` | ❌ مورد نقض |

---

### ۴. تحلیل LSP – اصل جایگزینی لیسکوف

| توضیح | کلاس/محل | نوع |
|--------|-----------|-----|
| هر جایی که `MessageSender` نیاز باشد، می‌توان نمونه‌ی `EmailSender` را قرار داد و رفتار کلی سیستم به‌درستی کار می‌کند. | `EmailSender` به‌عنوان پیاده‌سازی `MessageSender` | ✔️ مورد برقراری (ضعیف) |
| این اینترفیس فقط متد `sendEmail` دارد، در حالی که در سیستم نوع اعلان دیگری مثل `SMS` هم تعریف شده است. پیاده‌سازی‌های جدید (مثل `SmsSender`) مجبورند این متد مربوط به ایمیل را هم پیاده کنند ⇒ جایگزینی تمیز و طبیعی برای انواع مختلف پیام‌رسان سخت می‌شود. | اینترفیس `MessageSender` | ❌ مورد نقض ۱ |
| این کلاس مستقیماً به کلاس مشخص `EmailSender` وابسته است. اگر بخواهیم نوع دیگری از پیام‌رسان (مثلاً SMS) را جایگزین کنیم، باید خود این کلاس را تغییر دهیم؛ یعنی استفاده از پیاده‌سازی جدید بدون دست‌کاری مصرف‌کننده ممکن نیست. | `ReservationService` | ❌ مورد نقض ۲ |

---

## 3️⃣ گام سوم: بازطراحی بر اساس اصول شی‌گرایی (`Step-02-with-OOD`)

(در این بخش، توضیحات مربوط به تغییرات طراحی و بازطراحی کلاس‌ها در پوشه‌ی `Step-02-with-OOD` اضافه خواهد شد.)

---

## 4️⃣ گام چهارم: مقایسه‌ی تعداد تغییرات و ارزیابی طراحی

(در این بخش، تعداد تغییرات لازم برای افزودن قابلیت‌های جدید قبل و بعد از بازطراحی مقایسه خواهد شد.)

---

## 5️⃣ گام پنجم: نتیجه‌گیری

(در این بخش، جمع‌بندی نهایی از مزایا و نتایج رعایت اصول شی‌گرایی در طراحی سیستم ارائه خواهد شد.)
